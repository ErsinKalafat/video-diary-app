import { StyleSheet } from 'react-native';

/** expo-image's Image ignores NativeWind className, so its sizing lives here. */
export const styles = StyleSheet.create({
    image: { width: '100%', height: '100%' },
});
