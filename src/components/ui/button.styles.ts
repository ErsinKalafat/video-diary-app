import { StyleSheet } from 'react-native';

/** LinearGradient ignores NativeWind className, so its styling lives here. */
export const styles = StyleSheet.create({
    gradient: {
        alignItems: 'center',
        borderRadius: 16,
        paddingVertical: 14,
    },
});
